/**
 * @public
 */
export class Archive {
    static of(path, format = Archive.Format.MHTML) {
        return new Archive(path, format);
    }
    _path;
    _format;
    constructor(path, format) {
        this._path = path;
        this._format = format;
    }
    get path() {
        return this._path;
    }
    get format() {
        return this._format;
    }
    equals(value) {
        return (value instanceof Archive &&
            value._path === this._path &&
            value._format === this._format);
    }
    toJSON() {
        return {
            path: this._path,
            format: this._format,
        };
    }
}
/**
 * @public
 */
(function (Archive) {
    let Format;
    (function (Format) {
        Format["MHTML"] = "mhtml";
    })(Format = Archive.Format || (Archive.Format = {}));
})(Archive || (Archive = {}));
//# sourceMappingURL=archive.js.map