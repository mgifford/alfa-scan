import type { Device } from "@siteimprove/alfa-device";
import { Element } from "@siteimprove/alfa-dom";
import { Context } from "@siteimprove/alfa-selector";
import { Predicate } from "@siteimprove/alfa-predicate";
/**
 * Checks if an element has transparent background.
 * The element may be not fully transparent (e.g., have text) while still having transparent background.
 * @public
 */
export declare function hasTransparentBackground(device: Device, context?: Context): Predicate<Element>;
//# sourceMappingURL=has-transparent-background.d.ts.map