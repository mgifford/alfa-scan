import { Element } from "@siteimprove/alfa-dom";
import { Predicate } from "@siteimprove/alfa-predicate";
import { Style } from "../../style.js";
const { or } = Predicate;
/**
 * {@link https://html.spec.whatwg.org/#inert}
 *
 * @public
 */
export const isInert = (device) => or(isInertFromVisibility(device), isInertFromAttribute);
const isInertFromVisibility = (device) => Style.hasComputedStyle("visibility", (specified) => specified.value === "hidden" || specified.value === "collapse", device);
const isInertFromAttribute = (element) => element.isInert();
//# sourceMappingURL=is-inert.js.map