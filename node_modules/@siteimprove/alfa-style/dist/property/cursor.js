import { Keyword, List, Number, Token, Tuple, URL, } from "@siteimprove/alfa-css";
import { Parser } from "@siteimprove/alfa-parser";
import { Longhand } from "../longhand.js";
import { Resolver } from "../resolver.js";
const { left, map, option, pair, right, separated, zeroOrMore } = Parser;
const { parseComma, parseWhitespace } = Token;
const parseBuiltin = Keyword.parse("auto", "default", "none", "context-menu", "help", "pointer", "progress", "wait", "cell", "crosshair", "text", "vertical-text", "alias", "copy", "move", "no-drop", "not-allowed", "grab", "grabbing", "e-resize", "n-resize", "ne-resize", "nw-resize", "s-resize", "se-resize", "sw-resize", "w-resize", "ew-resize", "ns-resize", "nesw-resize", "nwse-resize", "col-resize", "row-resize", "all-scroll", "zoom-in", "zoom-out");
const parseCustom = map(pair(URL.parse, option(right(parseWhitespace, separated(Number.parse, parseWhitespace)))), ([url, coordinates]) => coordinates.isSome() ? Tuple.of(url, ...coordinates.get()) : url);
const parseCustomList = map(zeroOrMore(left(parseCustom, pair(parseComma, option(parseWhitespace)))), (list) => List.of(list, ","));
const parse = map(separated(parseCustomList, option(parseWhitespace), parseBuiltin), ([custom, fallback]) => Tuple.of(custom, fallback));
/**
 * {@link https://developer.mozilla.org/en-US/docs/Web/CSS/cursor}
 * @internal
 */
export default Longhand.of(Tuple.of(List.of([], ","), Keyword.of("auto")), parse, (value, style) => value.resolve(Resolver.length(style)), { inherits: true });
//# sourceMappingURL=cursor.js.map