import type { Slice } from "@siteimprove/alfa-slice";
import type { Token } from "@siteimprove/alfa-css";
import * as parser from "@siteimprove/alfa-parser";
import type { Longhands } from "./longhands.js";
import { Longhand } from "./longhand.js";
type Name = Longhands.Name;
/**
 * @internal
 */
export declare class Shorthand<N extends Name = never> {
    static of<N extends Name>(properties: Array<N>, parse: Shorthand.Parser<N>): Shorthand<N>;
    private readonly _properties;
    private readonly _parse;
    protected constructor(properties: Array<N>, parse: Shorthand.Parser<N>);
    get properties(): Iterable<N>;
    get parse(): Shorthand.Parser<N>;
}
/**
 * @internal
 */
export declare namespace Shorthand {
    type Parser<N extends Name = Name> = parser.Parser<Slice<Token>, Longhand.Default | Iterable<{
        [M in N]: readonly [M, Longhands.Declared<M>];
    }[N]>, string>;
}
export {};
//# sourceMappingURL=shorthand.d.ts.map