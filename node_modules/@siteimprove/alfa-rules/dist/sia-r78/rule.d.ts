import { Rule } from "@siteimprove/alfa-act";
import { Element } from "@siteimprove/alfa-dom";
import type { Option } from "@siteimprove/alfa-option";
import { Err, Ok } from "@siteimprove/alfa-result";
import type { Page } from "@siteimprove/alfa-web";
import { WithOtherHeading } from "../common/diagnostic.js";
declare const _default: Rule.Atomic<Page, Element<string>, {}, Element<string>>;
export default _default;
/**
 * @public
 */
export declare namespace Outcomes {
    const hasContent: (nextHeading: Option<Element>, currentLevel: number, nextLevel: number) => Ok<WithOtherHeading>;
    const hasNoContent: (nextHeading: Option<Element>, currentLevel: number, nextLevel: number) => Err<WithOtherHeading>;
}
//# sourceMappingURL=rule.d.ts.map