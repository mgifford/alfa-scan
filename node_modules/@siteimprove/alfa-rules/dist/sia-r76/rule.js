import { Rule } from "@siteimprove/alfa-act";
import { DOM } from "@siteimprove/alfa-aria";
import { Element, Namespace, Node } from "@siteimprove/alfa-dom";
import { EAA } from "@siteimprove/alfa-eaa";
import { Refinement } from "@siteimprove/alfa-refinement";
import { Err, Ok } from "@siteimprove/alfa-result";
import { Criterion } from "@siteimprove/alfa-wcag";
import { expectation } from "../common/act/expectation.js";
import { WithRole } from "../common/diagnostic/with-role.js";
import { Scope, Stability } from "../tags/index.js";
const { hasRole, isIncludedInTheAccessibilityTree, isPerceivableForAll } = DOM;
const { isElement, hasName, hasNamespace } = Element;
const { and, test } = Refinement;
export default Rule.Atomic.of({
    uri: "https://alfa.siteimprove.com/rules/sia-r76",
    requirements: [Criterion.of("1.3.1"), EAA.of("9.1.3.1")],
    tags: [Scope.Component, Stability.Stable],
    evaluate({ device, document }) {
        return {
            applicability() {
                return visit(document);
                function* visit(node, collect = false) {
                    if (test(and(isElement, hasNamespace(Namespace.HTML)), node)) {
                        if (test(hasName("table"), node)) {
                            // only collect cells of accessible tables
                            collect = test(isIncludedInTheAccessibilityTree(device), node);
                        }
                        if (collect &&
                            test(and(hasName("th"), isPerceivableForAll(device)), node)) {
                            yield node;
                        }
                    }
                    for (const child of node.children(Node.fullTree)) {
                        yield* visit(child, collect);
                    }
                }
            },
            expectations(target) {
                return {
                    1: expectation(test(hasRole(device, "columnheader", "rowheader"), target), () => Outcomes.HasHeaderRole(WithRole.getRoleName(target, device)), () => Outcomes.HasNoHeaderRole(WithRole.getRoleName(target, device))),
                };
            },
        };
    },
});
/**
 * @public
 */
export var Outcomes;
(function (Outcomes) {
    Outcomes.HasHeaderRole = (role) => Ok.of(WithRole.of(`The header element is a semantic header`, role));
    Outcomes.HasNoHeaderRole = (role) => Err.of(WithRole.of(`The header element is not a semantic header`, role));
})(Outcomes || (Outcomes = {}));
//# sourceMappingURL=rule.js.map