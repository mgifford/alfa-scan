import { Token } from "@siteimprove/alfa-css";
import type { Element } from "@siteimprove/alfa-dom";
import { type Option } from "@siteimprove/alfa-option";
import { Parser } from "@siteimprove/alfa-parser";
import { BaseSelector } from "../selector.js";
/**
 * {@link https://drafts.csswg.org/selectors/#universal-selector}
 *
 * @public
 */
export declare class Universal extends BaseSelector<"universal"> {
    static of(namespace: Option<string>): Universal;
    private static readonly _empty;
    static empty(): Universal;
    private readonly _namespace;
    protected constructor(namespace: Option<string>);
    get namespace(): Option<string>;
    matches(element: Element): boolean;
    equals(value: Universal): boolean;
    equals(value: unknown): value is this;
    [Symbol.iterator](): Iterator<Universal>;
    toJSON(): Universal.JSON;
    toString(): string;
}
/**
 * @public
 */
export declare namespace Universal {
    interface JSON extends BaseSelector.JSON<"universal"> {
        namespace: string | null;
    }
    function isUniversal(value: unknown): value is Universal;
    /**
     * {@link https://drafts.csswg.org/selectors/#typedef-type-selector}
     */
    const parse: Parser<import("@siteimprove/alfa-slice").Slice<Token>, Universal, string, []>;
}
//# sourceMappingURL=universal.d.ts.map