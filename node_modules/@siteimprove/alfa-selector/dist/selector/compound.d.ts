import { Array } from "@siteimprove/alfa-array";
import type { Element } from "@siteimprove/alfa-dom";
import type { Iterable } from "@siteimprove/alfa-iterable";
import type { Option } from "@siteimprove/alfa-option";
import { Parser } from "@siteimprove/alfa-parser";
import type { Context } from "../context.js";
import type { Selector } from "./index.js";
import { BaseSelector } from "./selector.js";
import { Class, Id, Simple, Type } from "./simple/index.js";
/**
 * {@link https://drafts.csswg.org/selectors/#compound}
 *
 * @public
 */
export declare class Compound extends BaseSelector<"compound"> {
    static of(...selectors: Array<Simple>): Compound;
    private readonly _selectors;
    private readonly _pseudos;
    private readonly _nonPseudos;
    private readonly _length;
    protected readonly _key: Option<Id | Class | Type>;
    protected constructor(selectors: Array<Simple>);
    get selectors(): Iterable<Simple>;
    get length(): number;
    matches(element: Element, context?: Context): boolean;
    equals(value: Compound): boolean;
    equals(value: unknown): value is this;
    [Symbol.iterator](): Iterator<Compound>;
    toJSON(): Compound.JSON;
    toString(): string;
}
/**
 * @public
 */
export declare namespace Compound {
    interface JSON extends BaseSelector.JSON<"compound"> {
        selectors: Array<Simple.JSON>;
    }
    function isCompound(value: unknown): value is Compound;
    /**
     * {@link https://drafts.csswg.org/selectors/#typedef-compound-selector}
     *
     * @internal
     */
    const parse: (parseSelector: Selector.Parser.Component) => Parser<import("@siteimprove/alfa-slice").Slice<import("@siteimprove/alfa-css").Token>, Simple | Compound, string, []>;
}
//# sourceMappingURL=compound.d.ts.map