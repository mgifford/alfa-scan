import { Element } from "@siteimprove/alfa-dom";
import { Context } from "../../../context.js";
import { type Relative, type Selector } from "../../index.js";
import { WithSelector } from "./pseudo-class.js";
/**
 * {@link https://drafts.csswg.org/selectors/#has-pseudo}
 */
export declare class Has extends WithSelector<"has", Relative> {
    static of(selector: Relative): Has;
    protected constructor(selector: Relative);
    matches(element: Element, context?: Context): boolean;
    /**
     * Whether an element match.
     *
     * @remarks
     * This method is further cached because it rebuilds new anchored selectors
     * on each call, so it wouldn't be able to use other caches, notably for
     * ancestors/descendants matching which can be expensive.
     */
    private _matches;
    [Symbol.iterator](): Iterator<Has>;
    equals(value: Has): boolean;
    equals(value: unknown): value is this;
    toJSON(): Has.JSON;
}
export declare namespace Has {
    interface JSON extends WithSelector.JSON<"has", Relative> {
    }
    const parse: (parseSelector: Selector.Parser.Component, withColon?: boolean) => import("@siteimprove/alfa-css").Parser<Has>;
}
//# sourceMappingURL=has.d.ts.map