import { Element } from "@siteimprove/alfa-dom";
import { Predicate } from "@siteimprove/alfa-predicate";
import { PseudoClassSelector } from "./pseudo-class.js";
/**
 * {@link https://drafts.csswg.org/selectors/#checked}
 */
export declare class Checked extends PseudoClassSelector<"checked"> {
    static of(): Checked;
    protected constructor();
    [Symbol.iterator](): Iterator<Checked>;
    /**
     * @privateRemarks
     * Checkedness and selectedness can change during the lifecycle of an element,
     * but we do not have access to that. We rely on the content attributes being
     * correctly set in the snapshot we test.
     */
    matches: Predicate<Element<string>, []>;
    toJSON(): Checked.JSON;
}
export declare namespace Checked {
    interface JSON extends PseudoClassSelector.JSON<"checked"> {
    }
    const parse: import("@siteimprove/alfa-css").Parser<Checked>;
}
//# sourceMappingURL=checked.d.ts.map