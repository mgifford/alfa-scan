import type { Element } from "@siteimprove/alfa-dom";
import type { Context } from "../../../context.js";
import type { Absolute, Selector } from "../../index.js";
import { WithSelector } from "./pseudo-class.js";
/**
 * {@link https://drafts.csswg.org/selectors/#zero-matches}
 */
export declare class Where extends WithSelector<"where"> {
    static of(selector: Absolute): Where;
    protected constructor(selector: Absolute);
    [Symbol.iterator](): Iterator<Where>;
    matches(element: Element, context?: Context): boolean;
    equals(value: Where): boolean;
    equals(value: unknown): value is this;
    toJSON(): Where.JSON;
}
export declare namespace Where {
    interface JSON extends WithSelector.JSON<"where"> {
    }
    const parse: (parseSelector: Selector.Parser.Component, withColon?: boolean) => import("@siteimprove/alfa-css").Parser<Where>;
}
//# sourceMappingURL=where.d.ts.map