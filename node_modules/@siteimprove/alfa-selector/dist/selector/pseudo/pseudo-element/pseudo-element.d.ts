import { type Parser as CSSParser } from "@siteimprove/alfa-css";
import type { Thunk } from "@siteimprove/alfa-thunk";
import { Specificity } from "../../../specificity.js";
import { WithName } from "../../selector.js";
export declare abstract class PseudoElementSelector<N extends string = string> extends WithName<"pseudo-element", N> {
    protected constructor(name: N, specificity?: Specificity, useContext?: boolean);
    equals(value: PseudoElementSelector): boolean;
    equals(value: unknown): value is this;
    toJSON(): PseudoElementSelector.JSON<N>;
    toString(): string;
}
export declare namespace PseudoElementSelector {
    interface JSON<N extends string = string> extends WithName.JSON<"pseudo-element", N> {
    }
    /**
     * Parses a non-functional, non-legacy pseudo-element (`::<name>`)
     */
    function parseNonLegacy<T>(name: string, of: Thunk<T>, withColon?: boolean): CSSParser<T>;
    /**
     * Parses a non-functional, legacy pseudo-element (`::<name>` or `:<name>`)
     */
    function parseLegacy<T>(name: string, of: Thunk<T>, withColon?: boolean): CSSParser<T>;
}
//# sourceMappingURL=pseudo-element.d.ts.map