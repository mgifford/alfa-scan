import { type Parser as CSSParser } from "@siteimprove/alfa-css";
import { Option } from "@siteimprove/alfa-option";
import type { Selector } from "../../index.js";
import { PseudoElementSelector } from "./pseudo-element.js";
/**
 * {@link https://w3c.github.io/webvtt/#the-cue-region-pseudo-element}
 */
export declare class CueRegion extends PseudoElementSelector<"cue-region"> {
    static of(selector?: Selector): CueRegion;
    private readonly _selector;
    protected constructor(selector: Option<Selector>);
    get selector(): Option<Selector>;
    [Symbol.iterator](): Iterator<CueRegion>;
    equals(value: CueRegion): boolean;
    equals(value: unknown): value is this;
    toJSON(): CueRegion.JSON;
    toString(): string;
}
export declare namespace CueRegion {
    interface JSON extends PseudoElementSelector.JSON<"cue-region"> {
        selector: Option.JSON<Selector>;
    }
    function parse(parseSelector: Selector.Parser.Component, withColon?: boolean): CSSParser<CueRegion>;
}
//# sourceMappingURL=cue-region.d.ts.map