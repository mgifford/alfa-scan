import { Array } from "@siteimprove/alfa-array";
import { Expression } from "../expression.js";
import type { Kind } from "../kind.js";
/**
 * @public
 */
export declare abstract class Function<T extends string = string, A extends Array<Expression> = Array<Expression>> extends Expression<T> {
    protected readonly _args: Readonly<A>;
    protected readonly _kind: Kind;
    protected constructor(type: T, args: Readonly<A>, kind: Kind);
    get args(): Readonly<A>;
    equals(value: Function<T, A>): boolean;
    equals(value: unknown): value is this;
    toJSON(): Function.JSON<T>;
}
/**
 * @public
 */
export declare namespace Function {
    interface JSON<T extends string = string> extends Expression.JSON<T> {
        arguments: Array<Expression.JSON>;
    }
}
//# sourceMappingURL=function.d.ts.map