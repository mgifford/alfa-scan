import { Parser } from "@siteimprove/alfa-parser";
import { type Parser as CSSParser } from "../../../syntax/index.js";
import { Unit } from "../../../unit/index.js";
import type { Numeric } from "../../numeric/index.js";
import { Expression } from "../expression.js";
import type { Kind } from "../kind.js";
import { Function } from "./function.js";
/**
 * {@link https://drafts.csswg.org/css-values/#calc-func}
 *
 * @public
 */
export declare class Calculation extends Function<"calculation", [Expression]> {
    static of(expression: Expression): Calculation;
    protected constructor(args: [Expression], kind: Kind);
    reduce<L extends Unit.Length = Unit.Length.Canonical, P extends Numeric = Numeric>(resolver: Expression.Resolver<L, P>): Expression;
    toString(): string;
}
/** @public */
export declare namespace Calculation {
    function isCalculation(value: unknown): value is Calculation;
    const parse: (parseSum: CSSParser<Expression>) => Parser<import("@siteimprove/alfa-slice").Slice<import("../../../syntax/token.js").Token>, Calculation, string, []>;
}
//# sourceMappingURL=calc.d.ts.map