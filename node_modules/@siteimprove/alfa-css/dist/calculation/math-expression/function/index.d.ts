import { type Parser as CSSParser } from "../../../syntax/index.js";
import { Expression } from "../expression.js";
import { Calculation } from "./calc.js";
import { Clamp } from "./clamp.js";
import { Max } from "./max.js";
import { Min } from "./min.js";
/** @public */
export type Function = Calculation | Clamp | Max | Min;
/** @public */
export declare namespace Function {
    const isCalculation: typeof Calculation.isCalculation;
    const isMax: typeof Max.isMax;
    const isMin: typeof Min.isMin;
    /**
     * Parses a math function in a calculation.
     *
     * @param parseSum - A parser for the sum production in a calculation.
     */
    function parse(parseSum: CSSParser<Expression>): CSSParser<Function>;
}
//# sourceMappingURL=index.d.ts.map