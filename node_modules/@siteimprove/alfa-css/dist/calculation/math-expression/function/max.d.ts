import { Array } from "@siteimprove/alfa-array";
import { Parser } from "@siteimprove/alfa-parser";
import { Result } from "@siteimprove/alfa-result";
import { type Parser as CSSParser, Token } from "../../../syntax/index.js";
import { Unit } from "../../../unit/index.js";
import { type Numeric } from "../../numeric/index.js";
import { Expression } from "../expression.js";
import type { Kind } from "../kind.js";
import { Function } from "./function.js";
/**
 * {@link https://drafts.csswg.org/css-values/#comp-func}
 *
 * @public
 */
export declare class Max extends Function<"max", [Expression, ...Array<Expression>]> {
    static of(first: Expression, ...expressions: ReadonlyArray<Expression>): Result<Max, string>;
    protected constructor(args: [Expression, ...Array<Expression>], kind: Kind);
    reduce<L extends Unit.Length = Unit.Length.Canonical, P extends Numeric = Numeric>(resolver: Expression.Resolver<L, P>): Expression;
    toString(): string;
}
/** @public */
export declare namespace Max {
    function isMax(value: unknown): value is Max;
    const parse: (parseSum: CSSParser<Expression>) => Parser<import("@siteimprove/alfa-slice").Slice<Token>, Max, string, []>;
}
//# sourceMappingURL=max.d.ts.map