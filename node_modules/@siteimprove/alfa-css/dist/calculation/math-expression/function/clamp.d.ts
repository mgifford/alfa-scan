import { Parser } from "@siteimprove/alfa-parser";
import { Result } from "@siteimprove/alfa-result";
import { type Parser as CSSParser, Token } from "../../../syntax/index.js";
import { Unit } from "../../../unit/index.js";
import { type Numeric } from "../../numeric/index.js";
import { Expression } from "../expression.js";
import type { Kind } from "../kind.js";
import { Function } from "./function.js";
/**
 * {@link https://drafts.csswg.org/css-values/#comp-func}
 *
 * @public
 */
export declare class Clamp extends Function<"clamp", [
    Expression,
    Expression,
    Expression
]> {
    static of(min: Expression, value: Expression, max: Expression): Result<Clamp, string>;
    protected constructor(min: Expression, value: Expression, max: Expression, kind: Kind);
    get min(): Expression;
    get value(): Expression;
    get max(): Expression;
    reduce<L extends Unit.Length = Unit.Length.Canonical, P extends Numeric = Numeric>(resolver: Expression.Resolver<L, P>): Expression;
    toString(): string;
}
/** @public */
export declare namespace Clamp {
    function isClamp(value: unknown): value is Clamp;
    const parse: (parseSum: CSSParser<Expression>) => Parser<import("@siteimprove/alfa-slice").Slice<Token>, Clamp, string, []>;
}
//# sourceMappingURL=clamp.d.ts.map