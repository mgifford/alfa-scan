/**
 * @internal
 */
export var Text;
(function (Text) {
    function indent(text, indent = "  ") {
        return text.replace(/^/gm, typeof indent === "string" ? indent : " ".repeat(indent));
    }
    Text.indent = indent;
    function wrap(text, length = 80) {
        if (text.length > length) {
            let target = length;
            while (target > 0 && !/\s/.test(text[target])) {
                target--;
            }
            if (target > 0) {
                const left = text.substring(0, target);
                const right = text.substring(target + 1);
                return left + "\n" + wrap(right, length);
            }
        }
        return text;
    }
    Text.wrap = wrap;
})(Text || (Text = {}));
//# sourceMappingURL=text.js.map